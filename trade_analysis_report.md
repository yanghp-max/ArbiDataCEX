# CEX 双腿时间差分析报告日志

- 报告时间: 2026-06-13
- 数据来源: 会话中提供的逐笔成交/信号日志（无本地 CSV 文件）
- 分析目标:
  - 交易所 A 价格时间 - 交易所 B 价格时间
  - 交易所 A 价格时间 - 信号接收价差时间
  - 交易所 B 价格时间 - 信号接收价差时间

## 1. 字段与口径定义

基于现有字段：

- `lat_a_age_ms = t_recv - t_a`
- `lat_b_age_ms = t_recv - t_b`
- `t_a`: 交易所 A 价格时间
- `t_b`: 交易所 B 价格时间
- `t_recv`: 信号接收（本地决策）时间

则三类时间差可直接换算为：

1) 交易所 A 价格时间 - 交易所 B 价格时间

- `t_a - t_b = (t_recv - lat_a_age_ms) - (t_recv - lat_b_age_ms)`
- `t_a - t_b = lat_b_age_ms - lat_a_age_ms`

2) 交易所 A 价格时间 - 信号接收价差时间

- `t_a - t_recv = -lat_a_age_ms`

3) 交易所 B 价格时间 - 信号接收价差时间

- `t_b - t_recv = -lat_b_age_ms`

补充：

- `lat_leg_skew_ms` 可视为双腿时间错位绝对值，通常与 `|lat_b_age_ms - lat_a_age_ms|` 一致或接近。

## 2. 结论摘要（基于当前日志样本）

### 2.0 具体统计值（本次样本 n=171）

计算口径：

- `A-B = lat_b_age_ms - lat_a_age_ms`
- `A-接收 = -lat_a_age_ms`
- `B-接收 = -lat_b_age_ms`

统计结果（单位：ms）：

- `交易所A价格时间 - 交易所B价格时间 (A-B)`
  - 平均值: `14.39`
  - 最小值: `-508.00`
  - 最大值: `840.00`

- `交易所A价格时间 - 信号接收时间 (A-接收)`
  - 平均值: `-20.78`
  - 最小值: `-535.00`
  - 最大值: `-1.00`

- `交易所B价格时间 - 信号接收时间 (B-接收)`
  - 平均值: `-35.16`
  - 最小值: `-855.00`
  - 最大值: `-1.00`

解释：

- `A-B` 为正表示 A 的价格时间更晚（更接近信号接收时刻），为负表示 B 更晚。
- `A-接收`、`B-接收` 为负是正常现象（价格时间通常早于信号接收时间）。
- 从平均值看，本样本中 B 相比 A 更“旧”一些（B 的平均 age 更大）。

### 2.1 正负号拆分统计（均值按符号分别计算）

说明：以下“正均值”仅对 `>0` 样本求均值，“负均值”仅对 `<0` 样本求均值，`0` 单独计数。

- `交易所A价格时间 - 交易所B价格时间 (A-B)`
  - 正值样本数: `148`，正值均值: `24.35 ms`
  - 负值样本数: `12`，负值均值: `-95.33 ms`
  - 零值样本数: `11`

- `交易所A价格时间 - 信号接收时间 (A-接收)`
  - 正值样本数: `0`，正值均值: `N/A`
  - 负值样本数: `171`，负值均值: `-20.78 ms`
  - 零值样本数: `0`

- `交易所B价格时间 - 信号接收时间 (B-接收)`
  - 正值样本数: `0`，正值均值: `N/A`
  - 负值样本数: `171`，负值均值: `-35.16 ms`
  - 零值样本数: `0`

1. **双腿时间错位整体可控，但存在长尾。**
   - 常见样本下，`|t_a - t_b|` 多在几十毫秒内（0~30ms 常见）。
   - 长尾样本可见 100ms+、500ms+、800ms+ 级别错位。

2. **异常主要体现为“单腿变旧”。**
   - 不少异常并非 A/B 同时变慢，而是一腿 `lat_*_age_ms` 正常，另一腿明显抬升。
   - 这种不对称时延更容易造成“看起来有价差、成交后回吐”。

3. **与执行质量直接相关。**
   - 当 `|t_a - t_b|` 抬升时，信号质量下降，腿间成交基准不一致风险上升。
   - 建议把时间一致性作为开仓和加仓前置门槛，而非仅看价差百分比。

## 3. 风险解读

### 3.1 对开仓/加仓的影响

- 若一腿价格时间显著早于另一腿（旧数据腿），可能放大虚假价差，导致：
  - 开仓后净值回撤加速；
  - 平仓时“吃不到回归”，净 PnL 受损。

### 3.2 对平仓的影响

- 平仓阶段若仍使用显著错位行情，可能出现：
  - 理论可平但实际成交偏离；
  - 手续费后净收益被延迟和滑点吞噬。

### 3.3 对风控的影响

- 仅靠 `spread_ab_pct / spread_ba_pct` 不足以约束时延风险；
- 需要联动 `lat_a_age_ms / lat_b_age_ms / lat_leg_skew_ms` 做“数据新鲜度风控”。

## 4. 建议阈值（可先上线试运行）

### 4.1 硬阈值（Hard Gate）

- 开仓/加仓前要求：
  - `lat_a_age_ms <= 80`
  - `lat_b_age_ms <= 80`
  - `lat_leg_skew_ms <= 40`
- 任一不满足则拒绝下单或降级为仅观察。

### 4.2 软阈值（Soft Gate）

- 当任一条件触发时降低仓位或放宽成交预期：
  - `lat_a_age_ms > 50` 或 `lat_b_age_ms > 50`
  - `lat_leg_skew_ms > 25`

### 4.3 异常熔断建议

- 连续 N 笔（如 5 笔）出现 `lat_leg_skew_ms > 100`，建议暂停该 symbol 新开仓 1~3 分钟。

## 5. 建议新增监控面板

- 时延分位数：`lat_a_age_ms / lat_b_age_ms / lat_leg_skew_ms` 的 P50/P95/P99/Max
- 单腿健康度：`lat_a_ws_transit_ms` 与 `lat_b_ws_transit_ms` 的分腿对比
- 策略阶段耗时：`lat_stage_*` 关键阶段（fresh_tick/precheck/presend/order_send）
- 交易结果联动：按时延分桶统计 `net_pnl`、`failed_leg`、`leg_mismatch`

## 6. 建议的复盘维度（日报格式）

每个 symbol 每日输出：

1. 交易笔数、开平仓次数、净 PnL
2. `t_a - t_b` 的均值/中位数/P95/P99/最大值
3. `t_a - t_recv`（即 `-lat_a_age_ms`）分位数
4. `t_b - t_recv`（即 `-lat_b_age_ms`）分位数
5. 超阈值笔数（按 25/40/80/100ms 阈值）
6. Top 异常样本（包含 `symbol/action/order_id/fail_reason`）

## 7. 本次报告限制

- 当前为“无 CSV 文件、仅会话日志文本”的快速分析报告；
- 结论以口径确认与风险画像为主，精确分位数需落地脚本跑全量后补齐。

## 8. 下一步执行建议

1. 将这批文本落盘为 CSV（或直接接入现有日志管道）。
2. 按上述三类时间差输出分位统计和异常明细。
3. 先在模拟/小仓位启用时间阈值门控，观察一周后再固化参数。

